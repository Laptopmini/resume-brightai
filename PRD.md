# PRD: Shell Components (Nav, Footer, Buttons, Dividers)

## Tasks

- [x] Create `src/components/GradientButton.tsx` exporting a default React component `GradientButton` accepting props `{ href: string; children: React.ReactNode; variant?: 'midnight' | 'accent'; external?: boolean }`. Render an `<a>` with `data-testid="gradient-button"`, classes `inline-flex items-center justify-center px-6 py-3 rounded-full font-medium text-paper transition-transform hover:scale-[1.02]` plus `bg-gradient-midnight` when `variant !== 'accent'` else `bg-gradient-accent`. When `external` is true, set `target="_blank"` and `rel="noreferrer noopener"`. The button must use Tailwind classes only — no inline hex codes. Import nothing from `@/lib/motion`. `[test: npx jest tests/unit/create-gradient-button.test.tsx]`
- [x] Create `src/components/SectionDivider.tsx` exporting default component `SectionDivider` rendering a `<div data-testid="section-divider" className="h-px w-full max-w-content mx-auto bg-gradient-accent opacity-60" />`. No props. The component must use the `bg-gradient-accent` Tailwind utility from Ticket 1, not an inline gradient. `[test: npx jest tests/unit/create-section-divider.test.tsx]`
- [x] Create `src/components/NavBar.tsx` exporting default component `NavBar`. Render a `motion.nav` from `framer-motion` spreading `navStick` from `@/lib/motion`, with `data-testid="nav-bar"`, classes `sticky top-0 z-40 w-full bg-midnight-deep/80 backdrop-blur border-b border-white/5`. Inside render a `max-w-content mx-auto px-6 h-16 flex items-center justify-between` row: left side `<a href="#top" className="font-bold tracking-tight text-paper">Paul-Valentin Mini</a>`, right side an unordered list of anchor links to `#about`, `#experience`, `#projects`, `#education`, `#ai-credit` with labels `About`, `Experience`, `Projects`, `Education`, `How it's built`. Each link has class `text-paper-muted hover:text-paper text-sm font-medium`. List is `hidden md:flex gap-8`. Imports: `motion` from `framer-motion`, `navStick` from `@/lib/motion`. `[test: npx jest tests/unit/create-nav-bar.test.tsx]`
- [ ] Create `src/components/Footer.tsx` exporting default component `Footer`. Render a `<footer data-testid="site-footer" className="bg-midnight-deep border-t border-white/5">` containing a `max-w-content mx-auto px-6 py-12 flex flex-col md:flex-row items-start md:items-center justify-between gap-6` row. Left: `<p className="text-paper-muted text-sm">© 2026 Paul-Valentin Mini · San Francisco, CA</p>`. Right: a horizontal list of three links — `mailto:paul@emini.com` labeled `Email`, `https://www.linkedin.com/in/pvmini` labeled `LinkedIn`, `https://github.com/Laptopmini` labeled `GitHub` — each `text-paper-muted hover:text-paper text-sm`. External links use `target="_blank" rel="noreferrer noopener"`. Pull contact strings from `hero` exported by `@/content/resume`. `[test: npx jest tests/unit/create-footer.test.tsx]`
