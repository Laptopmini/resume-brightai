# PRD: Content Components (Hero, Cards, Grid, AI Credit)

## Tasks

- [x] Create `src/components/ProfileHeader.tsx` exporting default component `ProfileHeader`. Render a `motion.section` (id `top`) spreading `heroReveal` from `@/lib/motion`, classes `max-w-content mx-auto px-6 pt-32 pb-24 grid md:grid-cols-[1fr_280px] gap-12 items-center`. Left column: `<p data-testid="hero-eyebrow" className="text-cyan-blue text-sm font-medium uppercase tracking-[0.2em]">{hero.eyebrow}</p>` then `<h1 data-testid="hero-title" className="mt-4 text-5xl md:text-7xl font-extrabold tracking-tight text-paper">{hero.title}</h1>` then a `<span className="block mt-2 h-1 w-32 bg-gradient-accent rounded-full" />` underline then `<p data-testid="hero-tagline" className="mt-6 text-lg md:text-xl text-paper-muted max-w-xl">{hero.tagline}</p>` then `<p className="mt-4 text-paper-muted text-sm">{hero.location} · {hero.email} · {hero.phone}</p>` then a `<div className="mt-8 flex gap-4">` containing `<GradientButton href={`mailto:${hero.email}`} variant="midnight">{hero.cta}</GradientButton>` and `<GradientButton href={hero.linkedin} variant="accent" external>LinkedIn</GradientButton>`. Right column: `<img data-testid="hero-portrait" src={withBasePath('profile.png')} alt="Paul-Valentin Mini" className="w-[280px] h-[280px] object-cover rounded-full border border-white/10 shadow-2xl" />`. Imports: `motion` from `framer-motion`, `heroReveal` from `@/lib/motion`, `hero` from `@/content/resume`, `withBasePath` from `@/lib/basePath`, `GradientButton` from `@/components/GradientButton`. Note: this file imports `GradientButton.tsx` which is created in Ticket 2; this ticket depends on Ticket 1 only, but the file is consumed at composition time in Ticket 4 — ensure the import path is exactly `@/components/GradientButton`. `[test: npx jest tests/unit/create-profile-header.test.tsx]`
- [x] Create `src/components/ExperienceCard.tsx` exporting default component `ExperienceCard` accepting prop `entry: { company: string; location: string; role: string; period: string; bullets: string[]; stack: string[] }`. Render a `motion.article` spreading `cardHover` from `@/lib/motion`, classes `bg-midnight-card border border-white/5 rounded-2xl p-8`, with `data-testid="experience-card"`. Header row: `<h3 className="text-xl font-bold text-paper">{entry.company}</h3><p className="text-paper-muted text-sm">{entry.location}</p>`. Subheader: `<p className="mt-2 text-cyan-blue text-sm font-medium">{entry.role} · {entry.period}</p>`. Bullets: `<ul className="mt-6 space-y-3 list-disc list-outside pl-6 text-paper-muted">` mapping `entry.bullets` to `<li key={i} className="font-light leading-relaxed">{b}</li>`. Stack pills: `<div className="mt-6 flex flex-wrap gap-2">` mapping `entry.stack` to `<span key={i} data-testid="stack-pill" className="px-3 py-1 rounded-full text-xs bg-gradient-accent text-ink font-medium">{tech}</span>`. Imports: `motion` from `framer-motion`, `cardHover` from `@/lib/motion`. `[test: npx jest tests/unit/create-experience-card.test.tsx]`
- [x] Create `src/components/ProjectCard.tsx` exporting default component `ProjectCard` accepting prop `project: { name: string; kind: string; period: string; summary: string; bullets: string[]; stack: string[]; githubUrl: string }`. Render a `motion.article` spreading `cardHover` from `@/lib/motion`, classes `bg-midnight-card border border-white/5 rounded-2xl p-8`, `data-testid="project-card"`. Title row: `<h3 className="text-2xl font-bold text-paper">{project.name}</h3><p className="text-paper-muted text-sm mt-1">{project.kind} · {project.period}</p>`. Summary: `<p className="mt-4 text-paper-muted leading-relaxed">{project.summary}</p>`. Bullets: same `<ul>` shape as ExperienceCard. Stack pills: same `<div>` shape using `bg-gradient-accent` pills. Footer: `<a href={project.githubUrl} target="_blank" rel="noreferrer noopener" className="mt-6 inline-block text-cyan-blue hover:text-paper text-sm font-medium">View on GitHub →</a>`. Imports: `motion` from `framer-motion`, `cardHover` from `@/lib/motion`. `[test: npx jest tests/unit/create-project-card.test.tsx]`
- [x] Create `src/components/SkillsGrid.tsx` exporting default component `SkillsGrid`. Read `skills` from `@/content/resume`. Render a `<section data-testid="skills-grid" className="max-w-content mx-auto px-6 py-24 md:py-32">` with header `<h2 className="text-3xl md:text-4xl font-bold text-paper">Top Skills</h2>` then a `<div className="mt-10 grid md:grid-cols-2 gap-6">` mapping `skills` to a card per category: `<div key={cat} className="bg-midnight-card border border-white/5 rounded-2xl p-6"><h3 className="text-cyan-blue text-sm font-medium uppercase tracking-[0.2em]">{cat.category}</h3><div className="mt-4 flex flex-wrap gap-2">{cat.items.map(item => <span key={item} className="px-3 py-1 rounded-full text-xs bg-white/5 text-paper-muted">{item}</span>)}</div></div>`. Imports: `skills` from `@/content/resume`. `[test: npx jest tests/unit/create-skills-grid.test.tsx]`
- [x] Create `src/components/EducationList.tsx` exporting default component `EducationList`. Read `education` from `@/content/resume`. Render a `<section data-testid="education-list" className="max-w-content mx-auto px-6 py-24 md:py-32">` with `<h2 className="text-3xl md:text-4xl font-bold text-paper">Education & Certifications</h2>` then `<ul className="mt-10 space-y-4">` mapping each entry to `<li key={i} className="flex flex-col md:flex-row md:items-baseline justify-between gap-2 bg-midnight-card border border-white/5 rounded-2xl p-6"><span className="text-paper font-medium">{entry.title}</span><span className="text-paper-muted text-sm">{entry.detail}{entry.status ? ` · ${entry.status}` : ''}</span></li>`. Imports: `education` from `@/content/resume`. `[test: npx jest tests/unit/create-education-list.test.tsx]`
- [ ] Create `src/components/AICreditSection.tsx` exporting default component `AICreditSection`. Read `aiCredit` from `@/content/resume`. Render a `<section id="ai-credit" data-testid="ai-credit" className="relative max-w-content mx-auto px-6 py-24 md:py-32">`. Top accent: `<motion.div className="h-1 w-full bg-gradient-spectrum bg-[length:200%_100%] rounded-full" {...gradientShimmer} />` (spread `gradientShimmer` from `@/lib/motion`). Below: `<p className="mt-10 text-cyan-blue text-sm font-medium uppercase tracking-[0.2em]">{aiCredit.eyebrow}</p>` then `<h2 className="mt-3 text-3xl md:text-4xl font-bold text-paper max-w-3xl">{aiCredit.title}</h2>` then `<p className="mt-6 text-paper-muted leading-relaxed max-w-3xl">{aiCredit.body}</p>`. Workflow steps: `<ol className="mt-8 grid md:grid-cols-4 gap-4">` mapping `aiCredit.steps` to `<li key={s} className="bg-midnight-card border border-white/5 rounded-2xl p-6"><span className="text-cyan-blue font-medium">{String(i + 1).padStart(2, '0')}</span><p className="mt-2 text-paper">{s}</p></li>`. CTA: `<div className="mt-10"><GradientButton href={aiCredit.repoUrl} variant="midnight" external>{aiCredit.cta}</GradientButton></div>`. Imports: `motion` from `framer-motion`, `gradientShimmer` from `@/lib/motion`, `aiCredit` from `@/content/resume`, `GradientButton` from `@/components/GradientButton` (created in Ticket 2; this file imports it but does not create it). `[test: npx jest tests/unit/create-ai-credit-section.test.tsx]`
